import { FilterTimeSheetsPipe } from './filter-time-sheets.pipe';

describe('FilterTimeSheetsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTimeSheetsPipe();
    expect(pipe).toBeTruthy();
  });
});
