import { FilterMaterialsPipe } from './filter-materials.pipe';

describe('FilterMaterialsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMaterialsPipe();
    expect(pipe).toBeTruthy();
  });
});
