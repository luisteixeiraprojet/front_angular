import { FilterMyTimesheetsPipe } from './filter-my-timesheets.pipe';

describe('FilterMyTimesheetsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMyTimesheetsPipe();
    expect(pipe).toBeTruthy();
  });
});
