import { FilterMyAbsencesPipe } from './filter-my-absences.pipe';

describe('FilterMyAbsencesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMyAbsencesPipe();
    expect(pipe).toBeTruthy();
  });
});
